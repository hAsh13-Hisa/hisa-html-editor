# デスクトップ版 HTMLエディタ 要件定義書（ヒアリングシート）

本書は、Chrome拡張機能版「Hisa HTML Editor」をベースに、Dreamweaverの基本編集機能を代替できるデスクトップアプリケーションへバージョンアップするための要件定義・ヒアリングシートです。

該当する選択肢の `[ ]` を `[x]` に変更いただくか、記入欄に直接ご要望をご記入ください。編集完了後にお知らせいただければ、内容を反映して実装計画を策定いたします。

---

## 1. アプリケーション基盤（フレームワーク）

デスクトップアプリとして動作させるための基盤技術を選択してください。

- [x] **A. Electron（推奨）**
  - メリット: VS Codeと同等のリッチなエディタエンジン（Monaco Editor）の導入が容易。Node.jsによる完全なファイルアクセス、ローカルプレビュー用内蔵Webサーバーの立ち上げ、CSSファイル解析などが安定して構築可能。
  - デメリット: アプリサイズがやや大きくなる（約80〜100MB程度）。
- [ ] **B. Tauri**
  - メリット: Rust製で非常に軽量・高速（約10〜15MB程度）。
  - デメリット: 開発環境にRust/C++ビルドツールが必要。Monaco Editor等の重厚なWebライブラリとの連携で一部制約が出る場合がある。
- [ ] **C. その他 / こだわりなし**
  - 自由記述: 

---

## 2. エディタエンジン

Dreamweaverのような高度なコード補完やCSS連携を実現するためのコアエディタの選定です。

- [x] **A. Monaco Editor（VS Codeのエディタ部分を採用 / 推奨）**
  - HTML/CSS/JavaScriptの言語サーバーが内蔵されており、タグ補完、Emmet、構文ハイライト、CSSプロパティ補完、エラー検出が非常に高精度。
- [ ] **B. CodeMirror 5（現行のまま拡張）**
  - 現行コードをそのまま活用し、CodeMirror用のオートコンプリート・アドオンを追加していく構成。
- [ ] **C. CodeMirror 6（モダン版CodeMirrorに移行）**
- 補足・希望: 

---

## 3. タグ補完機能（HTML編集補助）

含めたいタグ補完・入力補助機能にチェックを入れてください。（複数選択可）

- [ ] **タグの自動補完**: `<` を入力した際に、HTML5タグ一覧をサジェスト
- [x] **自動閉じタグ**: `<div>` と入力した時点で自動的に `</div>` が挿入される
- [x] **終了タグの自動補完**: `</` を入力した際に、最も内側の対応タグ名が自動補完される
- [x] **属性の自動補完**: タグ内でスペースを入力した際に、対応する属性（`class`, `id`, `src`, `href`, `alt` 等）を候補表示
- [x] **Emmet（エメット）展開**: `ul>li*3` + Tabキー等で `<ul><li></li><li></li><li></li></ul>` に一括展開
- [x] **タグの囲み（Wrap with Abbreviation）**: 選択範囲を任意のタグ（例: `<p>` や `<a>`）で素早く囲む機能
- その他ご要望: 

---

## 4. CSS参照・連携機能（Dreamweaver代替の重要機能）

ご希望の機能にチェックを入れてください。（複数選択可）

- [x] **HTML内のCSSクラス名・ID補完**:
  - `<link rel="stylesheet" href="...">` で読み込んでいる外部CSSファイル、および `<style>` タグ内のクラス名・ID名を自動解析し、`class=""` や `id=""` 入力時にサジェストする。
- [x] **CSS定義へのジャンプ（Go to Definition）**:
  - HTMLタグ上のクラス名（例: `class="header-nav"`）で `F12` や `Ctrl+クリック` を押すと、該当CSSファイルの該当ルール定義行へジャンプ（または横に分割表示）。
- [x] **CSSカラーピッカー / クイック編集**:
  - カラーコード（`#ffffff` や `rgb(...)`）のホバー時にカラーピッカーを表示して直接色調整できる。
- [ ] **クイックインスペクタ（簡易CSSデザイナーパネル）**:
  - プレビュー上でクリックした要素に当たっているCSSルール（セレクタ、プロパティ一覧）をサイドパネル等で確認できる機能。
- その他ご要望: 

---

## 5. プレビュー画面・同期機能

現行の「プレビュー」と「エディタ」の連動機能の強化について。

- [x] **リアルタイムプレビュー（現行機能の維持）**: 入力と同時にプレビューへ反映
- [x] **双方向ハイライト・クリック選択（現行機能の維持）**:
  - プレビュー要素をクリック → エディタの対応タグへジャンプ
  - エディタのタグにカーソルを移動 → プレビュー側で該当要素に青枠ハイライト
- [x] **ローカル相対パス（画像・外部CSS）の正常表示**:
  - デスクトップアプリの内蔵ローカルサーバー経由により、`./images/logo.png` や `./css/style.css` などの相対パスをセキュリティ警告なしで確実にプレビュー表示する。
- [x] **プレビュー表示サイズの切り替え（レスポンシブ確認）**:
  - PC（100%）、タブレット（768px）、スマートフォン（375px）などのワンクリック切り替えボタン。
- その他ご要望: 
画像のURLにマウスカーソルがホバーしたときに、サムネイル画像を表示する。
---

## 6. ファイル・プロジェクト管理方式

ファイルの開き方や画面レイアウトについて。

- [x] **A. 単一ファイル方式（現行踏襲）**:
  - ツールバーの「開く」「保存」からHTMLファイル単体を都度開いて編集する。
- [ ] **B. プロジェクトフォルダ（サイト管理）方式（Dreamweaver風 / 推奨）**:
  - 「フォルダを開く」で作業フォルダ（サイトルート）を選択すると、左サイドバーにファイルツリー（HTML, CSS, JS, 画像）が表示され、クリックでファイルを切り替えてタブ編集できる。
  - ※外部CSSファイルも同時に開いて編集できるようになります。
- 補足・希望: 
htmlタグを修正中にページ内のCSSを修正したいとき、クイックエディットウインドウが開いて編集できるようにしたい。
---

## 7. 画面レイアウト・デザイン

- [x] **2画面分割（エディタ ＋ プレビュー）**:
  - [x] 左右分割（スプリッターで幅調整可能・現行踏襲）
  - [x] 上下分割への切り替えも可能にする
  - [x] エディタのみ / プレビューのみの全画面モード切替
- [x] **テーマ対応（ダーク / ライト / システム追従・現行踏襲）**
- 補足・希望: 

---

## 8. 配布形式・動作環境（OS）

- 対象OS:
  - [x] Windows
  - [x] macOS
  - [ ] Linux
- パッケージ形式:
  - [ ] インストーラー形式（.exe / セットアップウィザード）
  - [x] ポータブル形式（インストール不要でフォルダ解凍ですぐ動くzip版）

---

## 9. その他の追加要望・Dreamweaverのこの機能が欲しい等

ご自由にお書きください。

```text
（自由記述欄）
・
・
・
```
